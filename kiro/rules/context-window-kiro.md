# Context Window - Kiro-specific

When the context window is getting large, use `/compact` to summarise the conversation and free up space.

Before compacting, persist all important knowledge to memory - details may be lost during summarisation.
