# TODO Search

Search the workspace for TODOs. Present them to the user in the following format:

```
## {file}
- {type}: `{task}` [{line}]
```

Ensure that each todo is on it's own line by using `\n\n`.

When searching, don't include third party files. Include all file types.